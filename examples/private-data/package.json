{
  "name": "private-data-example",
  "version": "0.1.0",
  "private": true,
  "description": "Private storage example for Calimero JS SDK",
  "scripts": {
    "build:manual": "calimero-sdk build src/index.ts -o build/service.wasm",
    "clean": "rm -rf build"
  },
  "dependencies": {
    "@calimero/sdk": "workspace:*"
  },
  "devDependencies": {
    "@calimero/cli": "workspace:*",
    "typescript": "^5.3.3"
  }
}
