{
  "name": "curb-js",
  "version": "0.1.0",
  "private": true,
  "description": "Curb Chat built with Calimero JavaScript SDK",
  "scripts": {
    "build:manual": "calimero-sdk build src/index.ts -o build/curb.wasm && find build -type f ! -name 'curb.wasm' -delete && find build -type d -mindepth 1 -empty -delete",
    "clean": "rm -rf build"
  },
  "dependencies": {
    "@calimero-network/calimero-sdk-js": "workspace:*",
    "bs58": "^5.0.0"
  },
  "devDependencies": {
    "@calimero-network/calimero-cli-js": "workspace:*",
    "typescript": "^5.3.3"
  }
}
