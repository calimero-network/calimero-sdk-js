{
  "name": "nested-collections-demo",
  "version": "1.0.0",
  "description": "Demo showing automatic nested collection tracking",
  "main": "src/index.ts",
  "scripts": {
    "build:manual": "calimero-sdk build src/index.ts -o build/service.wasm",
    "build:ts": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@calimero/sdk": "workspace:*"
  },
  "devDependencies": {
    "@calimero/cli": "workspace:*",
    "typescript": "^5.0.0"
  }
}
